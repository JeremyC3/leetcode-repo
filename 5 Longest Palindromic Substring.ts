function longestPalindrome(s: string): string {
  
  // iterate through every element
  for(let i=0; i<s.length; i++){
    
  }
  // check for the element being the center
  // check for every character being between the center
};